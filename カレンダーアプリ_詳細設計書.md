# カレンダーアプリ 詳細設計書

## 1. システム概要
ゲームのリリース日を管理し、毎年のアニバーサリーおよびハーフアニバーサリーをカレンダー上に自動表示するWebアプリケーション。JSONファイルの直接編集を避け、Webブラウザ上のUI（表形式）からデータを追加・編集・削除できる機能を提供する。

## 2. システムアーキテクチャ
*   **フロントエンド言語・フレームワーク**: TypeScript, React.js (Vite)
*   **スタイリング**: Tailwind CSS
*   **データストア**: GitHubリポジトリ内の `data.json` ファイル
*   **データ更新方式**: フロントエンドから GitHub REST API を経由して直接 `data.json` をコミット・更新する（※要GitHub Personal Access Token）。
*   **ホスティング**: GitHub Pages
*   **CI/CD**: GitHub Actions (リポジトリへのPushを検知して自動ビルド・デプロイ)

## 3. 画面設計 (UI/UX)
アプリケーションは2つの画面（コンポーネント）で構成する。

### 3.1. カレンダー画面（ホーム）
*   **初期表示**: アクセスした時点の「今日」が含まれる年・月を表示する。
*   **ヘッダー部**:
    *   「前月（<）」「翌月（>）」ボタン
    *   「YYYY年 MM月」の表示
    *   「今日に戻る」ボタン
    *   「データ編集」画面への遷移ボタン
*   **カレンダー表示部**:
    *   日曜始まり（または月曜始まり）の7列グリッド表示。
    *   各日付セルに日付（数字）を表示。
    *   該当するアニバーサリー/ハーフアニバーサリーがある場合、セル内にバッジやテキストで「〇〇 周年」「〇〇 ハーフ」と表示する。

### 3.2. データ編集画面
*   **ヘッダー部**: 「カレンダーへ戻る」ボタン
*   **認証設定部**:
    *   GitHub Personal Access Token (PAT) の入力欄。
    *   ※ブラウザの LocalStorage に保存し、次回以降の入力を省略する。
*   **データ入力フォーム**:
    *   ゲームタイトル（テキストボックス）
    *   リリース日（日付ピッカー `YYYY-MM-DD`）
    *   「追加」ボタン
*   **データ一覧表**:
    *   列：ゲームタイトル、リリース日、操作（編集/削除ボタン）
    *   編集ボタン押下で、対象行のデータを入力フォームにセット。
*   **保存アクション**:
    *   「変更をGitHubに保存」ボタンを押下時、現在の表データをJSONに変換し、GitHub APIを利用してリポジトリ上の `data.json` を更新（コミット）する。

## 4. データ設計 (JSONスキーマ)
ファイルパス: `src/data/games.json`
```json
[
  {
    "id": "uuid-v4-string",
    "title": "ゲームタイトルA",
    "releaseDate": "2023-08-19"
  },
  {
    "id": "uuid-v4-string",
    "title": "ゲームタイトルB",
    "releaseDate": "2024-02-28"
  }
]
```
*   `id`: 一意の識別子。削除や編集時のキーとして使用。
*   `title`: ゲームの名称。
*   `releaseDate`: ISO 8601準拠の年月日（`YYYY-MM-DD`）。

## 5. ロジック・処理設計

### 5.1. アニバーサリー判定ロジック
カレンダーの描画対象月（例: M月）の日付（D日）に対して、全ゲームデータをループして以下を判定する。
*   `releaseDate` の 月(r_M) と 日(r_D) を取得。
*   **条件**: `M == r_M` かつ `D == r_D`
*   **表示テキスト**: `{title} アニバーサリー`

### 5.2. ハーフアニバーサリー判定ロジック
リリース日から半年後の日付を計算し、カレンダーの月・日と一致するか判定する。
*   **計算式**: リリース月の6ヶ月後を求める。`h_M = (r_M + 6) % 12` (※結果が0の場合は12)。
*   **日付(h_D)の境界値処理（重要）**:
    半年後の月に、リリース日と同じ「日」が存在しない場合（例：8月31日の半年後は2月31日がない）、月末日に補正する。
    *   例: リリース日が `2023-08-31` の場合、ハーフアニバーサリーは毎年 `2月28日`（閏年は `2月29日`）とする。
    *   JavaScriptの `Date` オブジェクトの仕様を活用するか、`date-fns` などの日付ライブラリを使用して安全に算出する。
*   **条件**: `M == h_M` かつ `D == h_D`
*   **表示テキスト**: `{title} ハーフアニバーサリー`

### 5.3. GitHub API 連携ロジック (JSON更新)
1.  リポジトリ所有者、リポジトリ名、ブランチ名（`main`）、対象ファイルパスを指定。
2.  GitHub API (`GET /repos/{owner}/{repo}/contents/{path}`) で現在の `data.json` の `sha` (ハッシュ値) を取得。
3.  変更後のJSON文字列を Base64 にエンコード。
4.  GitHub API (`PUT /repos/{owner}/{repo}/contents/{path}`) に `sha` と Base64データを渡してコミットを作成。

## 6. インフラ・CI/CD設計 (GitHub Actions)
`data.json` の更新や、ソースコードの修正が行われた（`main` ブランチにプッシュされた）タイミングで、自動的に静的ファイルをビルドし、GitHub Pagesにデプロイする。

ファイルパス: `.github/workflows/deploy.yml`
```yaml
name: Deploy React App to GitHub Pages

on:
  push:
    branches:
      - main # mainブランチへのプッシュ（APIからのdata.json更新コミットも含む）でトリガー

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18' # 任意のNode.jsバージョン

      - name: Install Dependencies
        run: npm install

      - name: Build Project
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist # Viteのデフォルト出力先
```

## 7. 開発・導入手順
1.  GitHubで新規リポジトリを作成。
2.  React (Vite) の初期環境を構築し、上記のコードを実装。
3.  GitHub Pagesの設定で、デプロイ元を `gh-pages` ブランチ（Actionsが生成するブランチ）に設定。
4.  ユーザー（あなた）は、自身のGitHubアカウントから「Personal Access Token (repo権限)」を発行し、アプリの編集画面に入力して使用開始。

---

**【設計のポイント解説】**
*   **「json直編集回避」の解決策:** 単なるHTML/JSではローカルファイルやGitHub上のファイルを直接書き換えられません。そのため、**GitHub APIを利用してアプリ上から直接リポジトリのJSONを更新（コミット）する**というアプローチをとっています。
*   **「GitHub Actionsで実装」の実現:** APIによるコミットが行われると、それがトリガーとなってGitHub Actionsが走り、**自動的に最新のJSONデータを読み込んだカレンダーが再ビルド・公開される**という全自動サイクルが完成します。
